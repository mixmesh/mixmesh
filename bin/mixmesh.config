[{kernel, [{logger_level, debug},
           {logger, [{handler, default, logger_std_h,
                      #{formatter => {logger_formatter, #{}}}}]}]},
%%           {logger, [{module_level, debug, [gaia_network_sender_serv,
%%                                            gaia_audio_source_serv,
%%                                            gaia_serv]}]}]},
 {mixmesh, [{mode, normal},
            {app_schemas, [{mixmesh, system_schema},
                           {nodis, nodis_schema},
                           {keydir, keydir_service_schema},
%%                           {gaia, gaia_schema},
                           {player, player_schema},
                           {mixmesh, mixmesh_control_schema},
                           {mixmesh, logs_schema}]}]}].
